name: Bug Report
description: File a bug report
title: "🐛 - "
labels: ["bug"]
assignees:
  - restray
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to fill out this bug report.
        If you have any question, think about opening a discussion first to discuss the community about it!
        And think to fill up the title 😉
  - type: checkboxes
    id: common-issues
    attributes:
      label: Common issues
      description: I've read the [common issues](https://github.com/qbcore-framework/qb-inventory/issues/266) ticket
      options:
        label: Did you think about checking on the [common issues](https://github.com/qbcore-framework/qb-inventory/issues/266) issue on GitHub ?
        required: true
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: What did you expected, what do you get on server/client?
      placeholder: Tell us what you see!
    validations:
      required: true
  - type: textarea
    id: reproducing
    attributes:
      label: What happened?
      description: Also tell us, what did you expect to happen? 
      placeholder: Tell us what you see! If you want to send a video, post it on youtube and paste the link in this field!
    validations:
      required: true
  - type: dropdown
    id: version
    attributes:
      label: Version
      description: What version of the inventory are you running?
      options:
        - old (Default)
        - main branch (Insiders)
        - dev branch (Developer)
        - v1.0.0-prerelease (Beta Testers)
    validations:
      required: true
  - type: dropdown
    id: server-os
    attributes:
      label: The server operating system (OS), make sure that you're on x64 or amd64
      multiple: true
      options:
        - Windows
        - Linux Debian Based (Debian, Ubuntu)
        - Linux Others
  - type: textarea
    id: server-logs
    attributes:
      label: Relevant server log output
      description: Please copy and paste any relevant server log output. This will be automatically formatted into code, so no need for backticks.
      render: shell
  - type: input
    id: client-logs
    attributes:
      label: Client console (F8) error screenshot
      placeholder: |
        Use https://imgur.com/upload or https://pasteboard.co/ to upload the screenshot, or just paste the logs here.
